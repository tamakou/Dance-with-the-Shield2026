using UnityEngine;

namespace DWS
{
    /// <summary>
    /// Attach this to the root of the shield object.
    /// Arrows treat any collider under this object as "shield" for blocking.
    /// </summary>
    public sealed class ShieldMarker : MonoBehaviour
    {
    }
}
